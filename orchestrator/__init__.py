"""Orchestrator: the Strands Graph (§1) plus the deterministic
human_gate (§2.4) and correction_executor (§2.5) steps. The correction
path lives HERE, never inside any LLM agent (build-contract §4)."""
